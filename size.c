#include <stdio.h>
#include "size.h"

int main() {
    printf("The size of struct A is ", sizeof(A));
    printf("The size of struct B is ", sizeof(B));
    printf("The size of struct C is ", sizeof(C));
}
