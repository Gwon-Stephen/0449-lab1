#include <stdio.h>
#include "size.h"

int main() {
    printf("The size of struct A is %zu\n", sizeof(A));
    printf("The size of struct B is %zu\n", sizeof(B));
    printf("The size of struct C is %zu\n", sizeof(C));

    return 0;
}
