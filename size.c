#include <stdio.h>
#include "size.h"

int main() {
    printf("The size of struct A is ", sizeof(A), "%s\n");
    printf("The size of struct B is ", sizeof(B), "%s\n");
    printf("The size of struct C is ", sizeof(C));
}
